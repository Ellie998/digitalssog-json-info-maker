import styled from "@emotion/styled";

const Container = styled.div`
  text-align: center;
  margin-top: 10px;
`;

const Header = () => {
  return (
    <Container>
      <h1>JSON INFO MAKER</h1>
    </Container>
  );
};

export default Header;
